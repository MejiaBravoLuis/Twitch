export const streamKey = (props) => {
    const { streamKey } = props
    return(
        <div className="settings settins-stream-container">
            <span>{streamKey}</span>
        </div>
    )
}